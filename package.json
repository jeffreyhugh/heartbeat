{
  "name": "heartbeat",
  "version": "0.1.0",
  "main": "index.js",
  "repository": "https://github.com/jeffreyhugh/heartbeat",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "app": "yarn workspace app dev",
    "app:dev": "yarn workspace app dev",
    "app:build": "yarn workspace app build",
    "app:start": "yarn workspace app start",
    "app:lint": "yarn workspace app lint",
    "app:lint:fix": "yarn workspace app lint:fix",
    "app:lint:strict": "yarn workspace app lint:strict",
    "app:typecheck": "yarn workspace app typecheck",
    "app:test:watch": "yarn workspace app test:watch",
    "app:test": "yarn workspace app test",
    "app:format": "yarn workspace app format",
    "app:format:check": "yarn workspace app format:check",
    "app:release": "yarn workspace app release",
    "app:push-release": "yarn workspace app push-release",
    "app:postbuild": "yarn workspace app postbuild",
    "app:prepare": "yarn workspace app prepare"
  },
  "engines": {
    "yarn": "@3.2.1"
  },
  "packageManager": "yarn@3.2.1"
}
